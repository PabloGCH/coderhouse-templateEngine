h2="Products"
if data.products.length > 0
	table(class="table table-striped")
		thead
			tr
				th="Name"
				th="Price"
				th
		tbody
			each val in data.products
				tr
					td=val.name
					td=val.price
					td
						img(src=val.imgUrl class="table-img")
else
	p="No products available"
